/*
const comida = 'pão na chapa';
const bebida = 'cafezinho';

if (bebida === 'cafezinho' && comida === 'pão na chapa') {
  console.log('Muito obrigado pela refeição :)');
} else {
  console.log('Acho que houve um engano com meu pedido');
} */

/*
  const conditionOne = true;
  const conditionTwo = false;

  console.log(conditionOne && conditionTwo);
*/

/*
const currentHour = 00;
var message = "";
    if (currentHour >= 22 ) {
        message = "Nao devemos comer nada, é hora de dormir";
    } else if(currentHour >= 18 && currentHour < 22 ){
        message =  "Rango da noite. PAPA"
    } else if ( currentHour >= 14 && currentHour < 18){
        message = "Vamos faer um bolo?"
    } else if ( currentHour >= 11 && currentHour <14 ){
        message = "Hora de almocar"
    } else if ( currentHour >= 4 && currentHour < 11 ){
        message = "Cafe da manha nativa mermao?"
    } else {
        message =  "ta dormindo e pesando em comida? gordo vai acorda e correr uns 11km"
    }
    console.log(message)
*/


